name: Deploy to Cloud Run (manual)

on:
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Authenticate to GCP
        uses: google-github-actions/auth@v1
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Setup gcloud
        uses: google-github-actions/setup-gcloud@v1

      - name: Configure project & region
        run: |
          gcloud config set project ${{ secrets.GCP_PROJECT }}
          gcloud config set run/region ${{ secrets.GCP_REGION }}

      - name: Build (example)
        run: |
          echo "Build step placeholder - adapte para seu build/push"
          # Ex: gcloud builds submit --tag gcr.io/${{ secrets.GCP_PROJECT }}/ordino-backend:${{ github.sha }}

      - name: Deploy (example)
        run: |
          echo "Deploy step placeholder - adapte para seu deploy"
          # Ex: gcloud run deploy ordino-backend \
          #   --image gcr.io/${{ secrets.GCP_PROJECT }}/ordino-backend:${{ github.sha }} \
          #   --region ${{ secrets.GCP_REGION }} --platform managed --allow-unauthenticated

